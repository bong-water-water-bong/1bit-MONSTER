# DSH Memory

- **ZERO-DMA FUSED CASCADE SILICON-VERIFIED (2026-08-28, commit ff4d7b3e)**: the
  full fused GU→SiLU→D single-launch design (h2 stays core-local, ZERO h2 DMA
  round-trip) now fires correctly on the NPU. M=8 K=2048 N_D=128 all-ones →
  C2 = 260096 everywhere, bad=0/1024, state=4 (4ms), reproducible. TWO fixes
  (both hidden because only the D-only probe had ever passed — h2=const, no GU):
  1) **wait=True on EVERY fill (AB + B_d)** — with 132 fills/shim and default
     wait=False, `dma_free_task` frees BD IDs while the previous DMA may still
     be in flight (16 BDs/shim) → launch deadlocks (state=8). wait=True awaits
     each single-BD task before freeing.
  2) **silu_quant_i8_fused_q22 all-rows** — it was a decode-M=1 kernel (row-0
     only, zeroed h2 rows 1-7) → C2 rows 1-7 = 0. Now loops all DIM_M rows
     with the r*8 microtiled offset (C1 layout (c/8)*64 + r*8 + (c%8)).
  Canonical artifacts committed: `final_cascade_fused.xclbin` (75776 B) +
  `insts_cascade_fused.txt`; probe `tests/fused_ab_probe.cpp` (AB/C2/B_d →
  groups 3/4/5); D-only regression (`orig_d_probe.cpp`) still C2=2048, bad=0.
  Recipe: `N_D=128 ./engine/npu/generators/build_iron_cascade.sh` (iron
  toolchain /home/bcloud/iron, OLD `Runtime()`+`with rt.sequence` API). This
  closes the #1919/#1920 zero-DMA saga: driver fixed (0.17.0 + TDR), then the
  fill-BD-reuse deadlock, then the silu row bug.
- This agent lives on the machine **strixhalo** (hostname confirmed: `strixhalo`).
  **strixhalo is a Ryzen box**: AMD Ryzen AI MAX+ 395 w/ Radeon 8060S
  (16 cores/32 threads) — the AMD "Strix Halo" APU, hence the hostname.
  The DeepSeek Harness agent runs on this Ryzen machine.
- Session workspace: `/home/bcloud/1bit-MONSTER` (the 1bit-MONSTER codebase).
- DeepSeek Harness Web GUI: http://127.0.0.1:3080
- **Private network**: strixhalo is part of a private LAN `192.168.50.0/24`
  (interface `eno1`, host IP `192.168.50.110`, gateway `192.168.50.1`, DHCP).
  Also has IPv6 `2607:fea8:ec5f:ff23:8647:9ff:fe77:7723`.
  Local name resolution: `strixhalo` → `127.0.1.1`.
- **Second machine — ryzen**: `192.168.50.100`, user `bcloud`, reachable via
  passwordless SSH key (`~/.ssh/id_ed25519`). AMD Ryzen 7 9800X3D (8c/16t),
  45 GiB RAM. Holds 1bit-MONSTER backups (`*.bundle`), a `1bit-monster` clone,
  `1bit-release`, a GitHub `actions-runner`, `ZAYA1-8B-Q4NX`, and docs like
  `STRIXHALO-DRIVER-PLAN.md` / `MIGRATION-STRIXHALO-OMARCHY.md`.
  NOTE: strixhalo's `/etc/ssh/ssh_config.d/20-systemd-ssh-proxy.conf` has bad
  permissions → always SSH with `-F /dev/null`.
- **Fleet / data landscape (2026-08-28, after exploration)**:
  - **strixhalo** (192.168.50.110, THIS PC, Ryzen AI Max+ 395): working machine. Repo
    `/home/bcloud/1bit-MONSTER`; models `~/models/` (zaya1-8b.q4nx, zaya-Q4_K.gguf, qwen2.5, 19 GB);
    `~/zaya_weights/` (33 GB raw layer bins); toolchains Xilinx/Vivado 269 GB, ROCm (rocm-therock),
    mlir-aie 25 GB, llvm-aie; NPU `/dev/accel0` (amdxdna); DSH sessions `~/.dsh/sessions/`.
    No `1bit-systems` here — the project became `1bit-MONSTER`.
  - **pi** `192.168.50.216` (user bcloud, sudo pw `bcloud`): BACKUP/EVIDENCE SERVER. ZFS pool
    `ZFSPool` (capital P!) `/ZFSPool/backups/{strixhalo: 2.14T, minisforum: 560G, ryzen: 175G,
    sliger}` — nightly rsync of each machine's `~/` + weekly snapshots (90-day). SD-card tarball
    snapshot `/mnt/strixhalo-backup/*.tar` (01-05, ~290 GB, 2026-08-05: 02-projects 231G,
    03-home-src 29G, 05-git-repos 13G). Session transcripts there. **Gotcha: `/ZFSpool` (lowercase
    p) is a DECOY dir — real data under capital-P.**
  - **ryzen** 192.168.50.100 (9800X3D): desktop; 1bit-MONSTER clone `~/projects/1bit-MONSTER`,
    coins-worker, backups.
  - **minisforum** ~192.168.50.61 (Windows workstation; 1bit Windows build path); also .185/.69.
  - **The project**: 1bit.MONSTER — reverse-engineered AMD XDNA 2 NPU in ~4 days (closed-source
    FastFlowLM → open C++23). Story = `docs/journey.md` (~600 hr, 2960 lines); evidence map =
    `docs/audit-trail.md`; RE reports in `docs/research/` (incl. `research/npu/AMD-XDNA-COLUMN-UNLOCK.md`);
    dead-end docs in `docs/archive/`; models incl. ZAYA1-74B/8B, Qwen3, Llama-3.2, Bonsai, Zamba2
    (on pi `1bit-systems/models/`).
- **Co-worker DSH agent (ryzen)**: runs DSH on ryzen (`http://127.0.0.1:3080`
  there, no REST API). Works in `~/projects/1bit-MONSTER` on ryzen, branch
  `fix/triage-round`, remote `fork` = `git@github.com:bong-water-water-bong/1bit-MONSTER.git`
  (22 commits ahead of my main 62b77e8f; merge-base = 62b77e8f → clean FF).
  Its triage: `.triage/TRIAGE-REPORT.md` + `docs/TRIAGE-ISSUES-2026-08.md` —
  27 GitHub issues triaged, 14+ fixes landed (#1832/#1865/#1872/#1874/#1882/...).
  Its remaining items need HW verification on strixhalo's NPU (#1865 byte-identity
  gate, #1872 direct-vector dequant re-validation, #1874 mmul transpose, #1832 q4nx decode).
  Session transcripts: `~/.dsh/sessions/--home-bcloud-projects-1bit-MONSTER--/` on ryzen
  (parser: `.agent-tools/summarize2.py`).
  **Sync hazard**: BOTH agents edit `engine/npu/generators/mm_kernel_reference.cc`
  (co-worker: #1865/#1874/#1872; me: BUG-002/003/005 cascade work, uncommitted).
- **Local strixhalo repo state (needs committing)**: 3 modified files
  (mm_kernel_reference.cc, n1_core_fused_gu_silu_d_iron.py, FUSED-H2-RELAY-DESIGN.md —
  verified fused-cascade work, BUG-005 silicon-verified) + untracked
  `docs/bug-reports/BUG-001..011.md` (local cascade triage; BUG-011 decision:
  single-launch premise REJECTED, p1/p2 two-launch is production path).
- **Sync done 2026-08-28**: local work committed (aecfad54) + BOTH co-worker branch
  states merged (fork state via 88de972c; the 16 newest commits from upstream
  `fix/triage-round` @ 954dc298 via 5c78007b) + origin/main (857b17f0). Coord doc
  `docs/AGENT-COORDINATION.md` created. Pushed: upstream `fix/triage-round` → a3518c1f
  (FF). **PR #1917** (fix/triage-round → main) opened via gh.
- **Zero-DMA cascade attempt (2026-08-28)**: the "C2=2048 exact" silicon-verified design
  is `aecfad54` = combined-AB (`matmul_i8_i32_ab`, one GU channel) + dedicated `of_b_d`
  (second channel). It was built with the `/home/bcloud/iron` toolchain (aiecc 167f34d88,
  a DIFFERENT older fork than `mlir-aie` e473a7248 or `mlir-aie-main` 8f03258fc=#3553),
  using `--dynamic-objFifos --alloc-scheme=basic-sequential`. The shared-B design (split
  A/B + shared `of_b`) does NOT fire on silicon (C2=0x5A) on any toolchain. The #3553
  "fill/BD regression" was a red herring — e473a7248 (pre-#3553) hangs identically.
  → generator `n1_core_fused_gu_silu_d_iron.py` reverted to combined-AB+dedicated-B_d on
  the NEW Runtime/Program API (uncommitted; +100/-74 vs a419d05e). Builds clean on the new
  aiecc (aie.xclbin 57 KB). p1/p2 two-launch remains production.
- **NPU state (2026-08-28, end of session)**: every iron launch hangs (run.wait() never
  returns) — even the verified aecfad54+/home/bcloud/iron build. Matches `ca9cd2ab`
  "each iron launch (reboot-only recovery)". Reset escalation FAILED: rmmod/modprobe
  (firmware reload only), aie-reset (mmap(/dev/mem) → MAP_FAILED), PCI FLR left the device
  `not ready after FLR` + `aie2_hw_resume: Start hardware failed -22`, `/dev/accel/accel0`
  GONE. → **NPU requires a full system reboot.** POST-REBOOT FIRST TEST: launch the
  prebuilt `engine/npu/xclbins/final_cascade_fused.xclbin` + `insts_cascade_fused.txt`
  with `/tmp/synth_ab/launch_ab` (AB=8912896, C2=1024×i32, B_d=262144, all-ones,
  --h2-const 42 → expect C2=86016). If C2=86016 → toolchain/design fine, hangs were NPU
  state; if still hangs → regression real, first try `--dynamic-objFifos` on the new aiecc.
- **POST-REBOOT (2026-08-28)**: still hangs on a FRESH NPU, for EVERY axis: designs
  (shared-B / combined-AB / ca9cd2ab cascade_d), toolchains (iron 167f34d88 / mlir-aie
  e473a7248 / mlir-aie-main 8f03258fc), harnesses (instr BO HOST_ONLY vs CACHEABLE),
  and `--unified --dynamic-objFifos --alloc-scheme=basic-sequential` (xclbin changes
  57529→78783 B but still hangs). XRT userspace 2.21.75, amdxdna driver 0.7.0, fw npu_7.sbin.
  Root hypothesis now: the aiecc is MISSING #3580 ("Lower a device's cores once without
  merging their objects") — the toolchain delta the summary ties to the launch reaching
  state=4. #3580 = upstream PR Xilinx/mlir-aie#3580, fetched as branch `pr3580`
  (eecabdabe). Its LLVM pin is ROCm/llvm-project `56bcc1871` (diverged from our 43574226b,
  so a FULL LLVM rebuild). IN PROGRESS: LLVM `56bcc1871` rebuild running in background;
  mlir-aie checked out at `pr3580` with the `(void)id` fix re-applied in
  AIECreatePathFindFlows.cpp. NEXT: after LLVM builds, `ninja -C mlir-aie-main/build aiecc`,
  then rebuild combined-AB design with `--unified --dynamic-objFifos
  --alloc-scheme=basic-sequential` and launch. Also open: ask co-worker (ryzen) if they
  already have the #3580 toolchain built (they recorded "state=4") — would skip the
  multi-hour LLVM rebuild.
- **#3580 DISPROVEN (2026-08-28)**: rebuilt LLVM 56bcc1871 + aiecc at `pr3580`, re-tested
  combined-AB and ca9cd2ab with `--unified --dynamic-objFifos --alloc-scheme`. xclbin is
  byte-identical to pre-#3580 (single-device; #3580 only changes MULTI-device merging).
  Still hangs. → single-launch zero-DMA is PROVEN BLOCKED; the only unexcluded variable is
  amdxdna driver 0.7.0 / fw npu_7.sbin. Filed GitHub issues: **#1919** (zero-DMA hang +
  full disambiguation matrix) and **#1920** (aie-reset cannot reset a hung NPU → reboot-only).
- **DRIVER ROOT-CAUSE (via amd/xdna-driver)**: the local IN-TREE amdxdna (0.7.0, kernel
  7.0.0-30) predates the TDR auto-reset — it exposes only `aie2_max_col`+`force_cmdlist`,
  while upstream amd/xdna-driver (commit 7004f1c) adds `tdr_timeout_ms`(default 2000) +
  `aie2_hw_reset()` (SMU power-cycle, no /dev/mem). → a hung iron job blocks `run.wait()`
  forever (no timeout, no auto-reset); upstream would time out @2s and power-cycle. FIX:
  build/install upstream amd/xdna-driver (dkms). Commented on #1919 + #1920.
- **BREAKTHROUGH (2026-08-28, late)**: reproduced **"C2=2048 exact"** under the CURRENT
  0.17.0 driver — decisive isolation of the zero-DMA hang. Method: extracted the
  VERIFIED `aecfad54` generator + `mm_kernel_reference.cc` + `build_iron_cascade.sh`
  (via `git show aecfad54:...`) into `/tmp/repro_ad54/`, built the D-only design
  (`--no-gu --h2-const=1 N_D=128`) with the STILL-PRESENT `/home/bcloud/iron` toolchain
  (aiecc=/home/bcloud/iron/bin/aiecc, peano=.../llvm-aie, PYTHONPATH=/home/bcloud/mlir-aie/install_tmp/python),
  and ran `orig_d_probe.cpp` (compiled `-lxrt_coreutil`) → **C2[0..11]=2048, bad=0/1024,
  sum=2097152**. Then ran the SAME harness against my PORTED `--no-gu` xclbin
  (`/tmp/w_nogu/`, NEW `Runtime(sequence_fn)`/`Program`/`TaskGroup` API) → **completes
  (state=4) but C2=0x5A5A5A5A (1515870810), bad=1024/1024 — data NEVER written**. Both
  re-verified; consistent. So: (1) the 0.17.0 driver + TDR is GOOD (state=4, no timeout —
  the NPU is now healthy; the earlier "state=8 timeout on every design" was the transient
  reboot-only NPU-degraded state, now cleared); (2) the IRON toolchain is GOOD; (3) the
  regression is the **NEW `Runtime(sequence_fn)`/`Program`/per-element `TaskGroup`-scoped
  fills** vs aecfad54's **OLD `Runtime()` + `with rt.sequence(...)` + `rt.start(*workers)`
  + simple fills** — the NEW API's fill/BD-delivery is BROKEN (C2 never written) while the
  OLD API delivers correctly (C2=2048). The old API is still supported by /home/bcloud/iron.
  PDI is structurally byte-identical across working/failing designs (cores ARE enabled — 8×
  MASK_WRITE core-enable @ row2 cols0-7 present; NOT a missing core-enable). FIX: restore
  aecfad54's generator (OLD Runtime API). Rebuild recipe + harness live in `/tmp/repro_ad54/`
  (build_donly.sh; orig_d_probe).
- **RESOLVED (2026-08-28)**: disabled Secure Boot (lockdown → `none`), built upstream
  amd/xdna-driver (7004f1c) → installed `updates/amdxdna.ko`, loaded as driver **0.17.0**
  (was 0.7.0) with **tdr_timeout_ms=2000**. Verified on NPU: the zero-DMA launch now returns
  from run.wait() in **2.0s, state=8 (ERT_CMD_STATE_TIMEOUT)** instead of hanging forever;
  dmesg shows `aie2_set_cmd_timeout`; a 2nd launch succeeds → NPU self-recovers (no reboot).
  `aie-reset` /dev/mem path ALSO works again (Secure Boot off). → #1919 + #1920 driver-side
  root cause fixed. REMAINING: the zero-DMA *compute* still deadlocks (C2=0x5A never written)
  — now surfaced as a clean 2s timeout, a separate fill/BD-delivery correctness bug, not driver.
  Rollback: `rm /lib/modules/7.0.0-30-generic/updates/amdxdna.ko && depmod -a && reboot`.
  NOTE: fork `bong-water-water-bong/1bit-MONSTER` `main` is a DIVERGENT personal
  workspace (60 unrelated commits: lemonade v11.8.0, 1bit-llm hw, analytics) — left
  untouched. Co-worker's active flow tracks UPSTREAM `fix/triage-round`, not the fork.
  Untracked scratch left in place: gc.mlir, gc_run, gc_run.cpp, xclbins/, _ab_out/.
