
*~
# ── Build artifacts ──
# ── Compiled binaries in tools/ (source is the truth) ──
# ── Environment / Config ──
# ── IDE / Editor ──
# ── Logs ──
# ── Model weight files (large, should not be committed) ──
# ── Python ──
# ── Stray / OS dirs ──
*.1bp
*.a
/adapters/
*.app
*.bin
build/
/build_clean
build_cmake/
build_test/
cmake-build-*/
/_deps
Desktop/
dist/
.DS_Store
*.dylib
*.egg
*.egg-info/
/engine/fusion/tokenize
/engine/fusion/zero_copy/test_pipeline_real
/engine/fusion/zig-out
.env
.env.local
*.exe
*.gguf
*.h1b
.idea/
*.log
logs/
models/*.1bp
models/*.gguf
models/FastFlowLM-Qwen3-0.6B-NPU2/
*.o
*.obj
*.onnx
out/
.project_doc_record/
*.pt
*.pth
*.pyc
__pycache__/
*.pyo
*.q4nx
/rust/target
*.safetensors
*.f32bin
*.so
*.swo
*.swp
Thumbs.db
*.trg
.vscode/

# Build directories
build_test/
build_clean/

# Python virtual envs
.venv/
venv/
.mcp.env
.mcp.json
.cache/

# ── Compiled binaries — source is the truth, never commit build outputs ──
# Extensionless ELF executables built in-tree from adjacent sources.
# (Keep files that carry an extension: .cpp/.h/.sh/.md/CMakeLists.txt/etc.)
tools/*
!tools/*.*
!tools/*/
engine/fusion/hybrid_test
engine/fusion/micro_bench
engine/fusion/simple_test
engine/fusion/verify_weights
engine/fusion/wmma_peak
engine/fusion/tokenize
engine/fusion/zero_copy/probe_contexts
engine/fusion/zero_copy/probe_multi_xclbin
engine/fusion/zero_copy/test_pipeline
engine/fusion/zero_copy/test_pipeline_real
engine/fusion/zero_copy/test_npu_ffn_real_weights
engine/fusion/zero_copy/test_zero_copy
engine/fusion/zero_copy/test_vk_dma_buf_import
engine/fusion/zero_copy/test_vk_attn_slice
engine/fusion/zero_copy/test_vkrt_dma_buf_import
engine/fusion/zero_copy/xclbin_health
engine/fusion/zero_copy/shaders/*.spv
engine/npu/tokenizer/tokenize
engine/npu/tokenizer/detokenize
/npu_engine

# ── Packaging build outputs (regenerated by deb/snap/appimage builds) ──
# Keep manifests/scripts; drop staged first-party binaries.
snap/prime/
packaging/binary/1bit-npu
packaging/binary/1bit-server
packaging/deb/usr/bin/1bit-npu
packaging/deb/usr/bin/1bit-server
packaging/appimage/AppDir/usr/bin/1bit-engine
.claude/

# Backup xclbins (8-tile variants)
engine/npu/xclbins/backup_8tile/
engine/npu/xclbins/backup_8tile_insts/
build-npu/
engine/npu/build-npu/
engine/npu/build-fresh/
build/*.ninja*
.workspace/

# Build caches / packaged artifacts (never commit; they balloon .git)
.zig-cache/
**/.zig-cache/
*.AppImage
# ...except the website's downloadable package set (site/downloads is deployed
# to 1bit.monster as-is; an ignored file never reaches the CI checkout)
!site/downloads/*.AppImage
scripts/.reconvert.lock
research/ws05-1bp-v2/RECONVERT-REPORT.md.tmp

# ── Local work artifacts (not for VCS) ──
node_modules/
.codewhale/
appimage-build/
appimagetool
heaptrack*
models/_src_q8/
npu-infer/mlir-aie
reasonix.toml
third_party/AMD-PACE/

# machine-specific HIP search-path shim (absolute symlinks violate #1043 policy)
.rocm-stub/

# ── Vivado / Versal DPU synthesis scratch (vendored IP + tool journals) ──
vpk180-dpu/
vivado.jou
vivado*.jou
vivado*.log
engine/npu/build-dbg/
engine/npu/build-dbg2/

# ── NPU pool probe binaries (build from source) ──
/engine/npu/pool/pool_probe
/engine/npu/pool/pool_kernel
/engine/npu/pool/pool_layer
models/*.htok

# Local workspace (not part of the repo)
.llm-wiki/
models/int8/
engine/npu/xclbins/v26_backup/
*.bak

# pixi env is a local toolchain cache (mojo==1.0.0 pinned in pixi.lock)
.pixi/
# ASan build tree (local debug)
build-asan/

# ── AIE/Vitis sim artifacts ──
.Xil/
x86simulator_output/
Work/
build-1715/
# ── hw/1bit-llm generated sim/build outputs (DESIGN.md §6: gitignored) ──
hw/1bit-llm/sim/
.gitnexus/

# packaging/iso build output (multi-GB, never committed)
packaging/iso/build/
*.iso

# packaging/flatpak build output (sources staged by build-flatpak.sh)
packaging/flatpak/xrt-libs/
packaging/flatpak/repo/
packaging/flatpak/build-dir/
packaging/flatpak/.flatpak-builder/
packaging/flatpak/*.flatpak
packaging/flatpak/glibc-libs/
packaging/flatpak/build
.triage/

# GC probe scratch from the zero-DMA hang investigation (see docs/bug-reports/) — keep out of the repo
/gc.mlir
/gc_run
/gc_run.cpp

# ── Vulkan shader build artifacts (gpu_attn_vk) ──
*.spv
/engine/fusion/gpu_attn_vk/test_vk_attn_layer
/engine/fusion/gpu_attn_vk/comp.spv
build100/

# ── Generated docs site (built by scripts/gen_docs_site.py) ──
/docs-site/
